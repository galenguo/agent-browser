apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-browser-aio
  namespace: agent-browser
  labels:
    app: agent-browser
    mode: all-in-one
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-browser
      mode: all-in-one
  template:
    metadata:
      labels:
        app: agent-browser
        mode: all-in-one
    spec:
      containers:
      - name: agent-browser
        image: ${REGISTRY_URL}/agent-browser:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
          name: api
          protocol: TCP
        - containerPort: 6080
          name: novnc
          protocol: TCP
        - containerPort: 5900
          name: vnc
          protocol: TCP
        env:
        - name: DEPLOYMENT_MODE
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: DEPLOYMENT_MODE
        - name: BROWSER_MODE
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: BROWSER_MODE
        - name: MAX_SESSIONS
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: MAX_SESSIONS
        - name: IDLE_TIMEOUT_SECONDS
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: IDLE_TIMEOUT_SECONDS
        - name: CDP_PORT
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: CDP_PORT
        - name: CDP_BIND_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: CDP_BIND_ADDRESS
        - name: PROFILE_STORAGE
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: PROFILE_STORAGE
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: LOG_LEVEL
        - name: HEADLESS
          valueFrom:
            configMapKeyRef:
              name: agent-browser-config
              key: HEADLESS
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: agent-browser-secret
              key: ANTHROPIC_API_KEY
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: agent-browser-secret
              key: OPENAI_API_KEY
        - name: OPENAI_BASE_URL
          valueFrom:
            secretKeyRef:
              name: agent-browser-secret
              key: OPENAI_BASE_URL
        volumeMounts:
        - name: profiles
          mountPath: /data/profiles
        - name: logs
          mountPath: /data/logs
        - name: shm
          mountPath: /dev/shm
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: profiles
        persistentVolumeClaim:
          claimName: agent-browser-profiles
      - name: logs
        persistentVolumeClaim:
          claimName: agent-browser-logs
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: 128Mi
