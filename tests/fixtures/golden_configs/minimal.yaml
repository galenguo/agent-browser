# Golden Master: minimal valid config (mode only)
# Generated by: generate_config(DeployConfig(mode="local"))
# with all other fields at defaults
# Used by: test_golden_master_minimal
deployment:
  mode: local
  configured_at: "2026-04-06T00:00:00Z"
browser:
  type: cloakbrowser
  cdp_url: "http://127.0.0.1:19222"
  headless: false
api:
  enabled: true
  port: 8000
stealth:
  enabled: true
  mode: full
skill:
  calling_mode: cli
  browser_mode: local
  intelligence: llm
  cdp_url: "http://127.0.0.1:19222"
  api_url: "http://127.0.0.1:8000"
  browser:
    headless: false
    default_timeout: 30000
  stealth:
    enabled: true
    mode: full
    warmup: false
