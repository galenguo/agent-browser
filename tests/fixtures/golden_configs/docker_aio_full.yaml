# Golden Master: docker-aio mode with full config
# Generated by: generate_config(DeployConfig(mode="docker-aio", ...))
# Used by: test_golden_master_docker_aio
deployment:
  mode: docker-aio
  os: linux
  arch: amd64
  configured_at: "2026-04-06T00:00:00Z"
  last_verified: null
browser:
  type: cloakbrowser
  cdp_url: "http://127.0.0.1:19222"
  headless: true
  max_sessions: 10
  idle_timeout: 1800
api:
  enabled: true
  host: "0.0.0.0"
  port: 8000
stealth:
  enabled: false
  mode: vanilla
docker:
  registry: "ghcr.io/myorg"
  image_tag: "v2.0"
  shm_size: "512Mi"
  resource_limits:
    memory: "8Gi"
    cpu: "4000m"
skill:
  calling_mode: api
  browser_mode: local
  intelligence: llm
  cdp_url: "http://127.0.0.1:19222"
  api_url: "http://0.0.0.0:8000"
  browser:
    headless: true
    default_timeout: 30000
  stealth:
    enabled: false
    mode: vanilla
    warmup: false
