# Golden Master: k8s-distributed mode
# Generated by: generate_config(DeployConfig(mode="k8s-distributed", ...))
# Used by: test_golden_master_k8s_distributed
deployment:
  mode: k8s-distributed
  os: linux
  arch: amd64
  configured_at: "2026-04-06T00:00:00Z"
  last_verified: null
browser:
  type: playwright
  cdp_url: "http://gateway.cdp:19222"
  headless: true
  max_sessions: 20
  idle_timeout: 3600
api:
  enabled: true
  host: "0.0.0.0"
  port: 8000
stealth:
  enabled: true
  mode: full
k8s:
  namespace: "stealth-browser-prod"
  context: "prod-cluster"
  registry: "ghcr.io/myorg"
  storage_class: "ssd-fast"
  replicas: 3
skill:
  calling_mode: api
  browser_mode: local
  intelligence: llm
  cdp_url: "http://gateway.cdp:19222"
  api_url: "http://0.0.0.0:8000"
  browser:
    headless: true
    default_timeout: 30000
  stealth:
    enabled: true
    mode: full
    warmup: false
