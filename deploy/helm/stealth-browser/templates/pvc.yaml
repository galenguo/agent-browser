{{- if .Values.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "stealth-browser.fullname" . }}-profiles
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stealth-browser.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.profiles.accessMode }}
  {{- if .Values.persistence.profiles.storageClass }}
  storageClassName: {{ .Values.persistence.profiles.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.profiles.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "stealth-browser.fullname" . }}-logs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stealth-browser.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.logs.accessMode }}
  {{- if .Values.persistence.logs.storageClass }}
  storageClassName: {{ .Values.persistence.logs.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.logs.size }}
{{- end }}
