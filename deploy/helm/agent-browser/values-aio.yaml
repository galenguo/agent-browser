# All-in-One 模式配置

deploymentMode: all-in-one

aio:
  replicaCount: 1
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "2Gi"
      cpu: "2000m"

config:
  maxSessions: 10
