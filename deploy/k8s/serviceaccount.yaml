# RBAC: API Pod needs permissions for K8s pool management + shared state
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-browser-api
  namespace: agent-browser
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: agent-browser-api-role
  namespace: agent-browser
rules:
  # List/watch browser pods for pool allocation
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  # Read/write shared state ConfigMap (CAS-based distributed coordination)
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: ["agent-browser-state"]
    verbs: ["get", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: agent-browser-api-binding
  namespace: agent-browser
subjects:
- kind: ServiceAccount
  name: agent-browser-api
roleRef:
  kind: Role
  name: agent-browser-api-role
  apiGroup: rbac.authorization.k8s.io
