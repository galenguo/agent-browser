# VNC per-pod VirtualService removed.
# VNC traffic now routes through the ClusterIP service (stealth-browser-cp:8000).
# FastAPI handles cross-pod proxying internally using the pod index embedded in the VNC token.
