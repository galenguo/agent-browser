apiVersion: v1
kind: Service
metadata:
  name: agent-browser-cp
  namespace: agent-browser
  labels:
    app: agent-browser-cp
spec:
  type: ClusterIP
  selector:
    app: agent-browser-cp
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
