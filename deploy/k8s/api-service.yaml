apiVersion: v1
kind: Service
metadata:
  name: stealth-browser-api
  namespace: stealth-browser
  labels:
    app: stealth-browser
    component: api
    mode: distributed
spec:
  type: NodePort
  selector:
    app: stealth-browser
    component: api
    mode: distributed
  ports:
  - name: api
    port: 8000
    targetPort: 8000
    nodePort: 30801
    protocol: TCP
