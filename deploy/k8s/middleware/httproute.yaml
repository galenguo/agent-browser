apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: agent-browser-route
  namespace: middleware
  labels:
    app: agent-browser
spec:
  hostnames:
  - agent-browser.vpc-dale.gimcyun.com
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: istio-gateway
    namespace: istio-system
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: agent-browser
      port: 8000
      weight: 100
    matches:
    - path:
        type: PathPrefix
        value: /
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: agent-browser-vnc-route
  namespace: middleware
  labels:
    app: agent-browser
spec:
  hostnames:
  - agent-browser-vnc.vpc-dale.gimcyun.com
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: istio-gateway
    namespace: istio-system
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: agent-browser
      port: 6080
      weight: 100
    matches:
    - path:
        type: PathPrefix
        value: /
