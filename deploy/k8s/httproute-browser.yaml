apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: browser-route
  namespace: stealth-browser
spec:
  parentRefs:
    - name: stealth-browser-gateway
      sectionName: web    # Must match Gateway listener name
  hostnames:
    - "b.stealth-browser.local"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: stealth-browser-browser
          port: 80
